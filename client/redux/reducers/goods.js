const GET_GOODS = 'GET_GOODS'

const initialState = {
    listOfGoods: []
}

export default = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export function getGoods() {

    return (dispatch) => {
        
    }
}